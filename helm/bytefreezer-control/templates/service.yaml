apiVersion: v1
kind: Service
metadata:
  name: {{ include "bytefreezer-control.fullname" . }}
  labels:
    {{- include "bytefreezer-control.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bytefreezer-control.selectorLabels" . | nindent 4 }}